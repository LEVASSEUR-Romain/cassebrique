/* import createMap from "../../../app/model/map/createMap.js";
test("map avec deux boss", () => {
  const map = [
    [
      {
        id: 1,
        difficulty: 1,
        type: "mob",
      },
      {
        id: 2,
        difficulty: 1,
        type: "mob",
      },
    ],
  ];
  const commons = {
    // map
    borderMapLeftRight: 2, //%
    borderMapTopBottom: 2, //%
    widhtItemMap: 6, //%
    heightItemMap: 6, //%
  };
  const result = {};
  expect(createMap(map, commons).toEqual(result));
}); */
